import React, { useState } from 'react';

const NASA = (props) => {
    if (props.url){
        //fetch to get image, return in div
        //blob research
    }

    return(
        <div>
            <img src='#'></img>
        </div>
    );
};

export default NASA;